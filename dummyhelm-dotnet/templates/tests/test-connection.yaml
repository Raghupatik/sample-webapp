apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dummyhelm-dotnet.fullname" . }}-test-connection"
  labels:
    {{- include "dummyhelm-dotnet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dummyhelm-dotnet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
